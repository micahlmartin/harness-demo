apiVersion: extensions/v1beta1
kind: Ingress
metadata:
 name: {{ include "httbin.fullname" . }}-svc-stage
 annotations:
  kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - http:
     paths:
      - backend:
          serviceName: {{ include "httbin.fullname" . }}
          servicePort: 80
