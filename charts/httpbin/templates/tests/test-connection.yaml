apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "httbin.fullname" . }}-test-connection"
  labels:
    {{- include "httbin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "httbin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
